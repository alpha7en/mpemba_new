# Revision log — «referee report» fixes

Дневник изменений с начала работы над замечаниями из дружеского referee report
(файл `REFEREE_REPORT.pdf`, получен 2026-07-19). Каждый блок = отдельный коммит с
префиксом `[rev]` в сообщении. Базовая точка до всех правок помечена git-тегом
**`pre-referee-fixes`** (= коммит `b3b5651 repo hygiene`).

## Как откатывать

| задача | команда |
|---|---|
| посмотреть всё, что сделано с начала правок | `git log --oneline pre-referee-fixes..HEAD` |
| откатить ОДИН блок (обратным коммитом, история цела) | `git revert <hash из таблицы ниже>` |
| посмотреть состояние «до правок» не трогая ничего | `git switch --detach pre-referee-fixes` (назад: `git switch main`) |
| откатить ВСЁ до состояния «до правок» (обратимо, история цела) | `git revert --no-commit pre-referee-fixes..HEAD && git commit -m "rollback referee fixes"` |

Данные вне git (свип `sweep_rightmost_10x10.npz`, чекпоинт pkl) откатывать не нужно:
легаси-архив `spectrum_sweep_10x10.npz` не перезаписывался, новый лежит рядом под
другим именем; фигурные скрипты переключаются на новый архив только явной правкой.

## Реестр коммитов ревизии

| хэш | блок | краткое содержание |
|---|---|---|
| `dc0ee14` | A (CF-1) | точные спектральные проекции Fig. 2 + контроль реконструкции + правки текста |
| `421c252` | B1-инфра (CF-2) | rightmost-решатель + верификация 61/61 и 4/4 + resumable-драйвер свипа |
| `2295c4c` | E | литература: 8 записей + атрибуция механизма + 6 правок текста |
| `3acd935` | F | адекватность модели: дефазинг/потери/единицы/кондиционирование |
| `9cff802` | план | revision_plan.md: полная инвентаризация + дерево решений |

Правила ведения: одна запись на блок работы; фиксируем ЧТО изменено (файлы),
ПОЧЕМУ (какое замечание закрывает), КАК проверено, и что при этом НЕ изменилось
(контроль). Хэш коммита добавляется в реестр сразу после коммита блока.

---

## 2026-07-19 — Блок A: точные спектральные проекции (замечание CF-1)

**Проблема (подтверждена):** попарная формула c_k = ⟨w_k|ρ⟩/⟨w_k|v_k⟩ некорректна внутри
вырожденных мультиплетов регулярной решётки (LAPACK не выдаёт биортогонально спаренные
лево/правые векторы). Ошибка реконструкции для «Coherent diag.» достигала ~1e3 %.

**Изменено:**
- `src/qdyn_research/metrics.py` — ДОБАВЛЕНЫ (аддитивно, легаси не тронуто):
  `factor_mode_basis` (LU от V), `project_rho_on_modes_exact` (решение V·c = vec(ρ)
  с ≤2 шагами итерационного уточнения; возвращает невязку реконструкции).
- `scripts/fig2_projection_bars_and_modes.py` — v2: точные проекции; кластеризация
  спектра на вырожденные мультиплеты (tol 1e-6 → 2613 кластеров); бары = базис-
  инвариантные веса мультиплетов ||V_μ c_μ||²; жёсткий контроль невязки (>1e-6 → отказ).
- `scripts/precalc/fig2_projections_exact.npz` — новый кэш (коэффициенты, невязки,
  спектр, кластеры, инвариантные веса). Легаси-кэш `fig2_projections.npz` сохранён
  для сравнения.
- `paper/figures/new_tabled_10x10_bar_chart_log_ENG_new_8STATES.pdf` — перегенерирован.
- `paper/main.tex` — 3 правки (одобрены): стр. ~271 честное описание нормировки
  (валидность попарной формулы + точное решение + контроль ≤1e-10); стр. ~273 описание
  W_{k'} (веса мультиплетов, базис-инвариантность); подпись Fig. 2 (W_k → W_{k'}).

**Результаты пересчёта:** невязки 7 состояний ~1e-13–1e-15; «Coherent diag.» 5.7e-11
(max|c| = 2.45e5 — почти-дефектный квадруплет, потолок ε·||c||). Веса 7 некогерентных
состояний практически не изменились (TV-расстояние 0.00–0.10); «Coherent diag.»
изменился на 92 % (TV=0.92) — старая панель была артефактом.

**Не изменилось (контроль):** Fig. 3 (пересечение 2×5) не зависит от разложения —
считается прямым интегрированием ODE; проверено по кэшу fig3: в окне t≤4 существует
ровно одна пересекающаяся пара (Coherent diag. × Opp. corners, t*=2.09). Селективность
7 смесей подтверждена точной математикой. Алгоритмическая секция и бенчмарк не затронуты.

**Отложено:** формулировка зелёного абзаца стр. ~279 («symmetry selection rules») —
ждёт одобрения пользователя (пункт 3).

---

## 2026-07-19 — Блок B1 (инфраструктура): корректная идентификация медленнейших мод (CF-2)

**Проблема (подтверждена точно):** легаси-свип (shift-invert при σ≈0, k=4) упорядочивал моды
по |λ| и физически не мог найти медленнейшие осциллирующие мультиплеты: на регулярной 10×10
истинно правый квадруплет λ = −0.080327±0.239009i — лишь 1174-й по |λ| (нужно k≥1174).
Расхождение −0.0803 (Fig.2) vs −0.0811 (Fig.7a), найденное рецензентом, воспроизведено.

**Отвергнутый вариант:** мульти-таргетный shift-invert с комплексными σ — осциллирующая
полоса широка и размерно-зависима (6×6: Im∈{0.28,0.40,0.68,1.12,1.52}; 10×10: 0.19–0.27),
сетка таргетов = хрупкое гадание.

**Решение:** экспоненциальное спектральное преобразование — Арнольди на пропагаторе e^{LT}
(|e^{λT}| = e^{Re λ·T} ⇒ доминирующие моды = max Re, без знания полосы). λ восстанавливается
рэлеевским фактором; каждая мода валидируется невязкой ≤1e-8 внутри решателя.

**Изменено (аддитивно, легаси цело):**
- `src/qdyn_research/spectral.py` + `analyze_liouvillian_modes_rightmost` (T=25, ncv=4k).
- `src/qdyn_research/npz_io.py` + `run_single_sparse_rewiring_job_rightmost` (та же схема
  хранения + массив невязок).
- `scripts/verify_rightmost_solver.py` — верификационный люк (6×6, 13 p × 5 сидов, плотная
  истина; жёсткие критерии: правая мода + топ-4 различимые группы; кратности/λ*-партнёры
  не требуются — у партнёра сопряжённые диагональные веса ⇒ IPR/overlap тождественны).
- `scripts/run_sweep_rightmost.py` — драйвер: почекпоинт на каждое p (resume), невязки в
  архиве, вывод sweep_rightmost_10x10.npz (имя НАМЕРЕННО вне глоба fig6–8 — переключение
  рисунков будет явным шагом).

**Верификация (полный след):**
- Итерация 1 (k=12): 47/61 PASS; 14 провалов = потерянные копии в почти-вырожденных
  кластерах (однокурсовой Крылов). Правая мода найдена на ВСЕХ 61.
- Исправления: критерий приведён к физике (различимые группы), решатель усилен (k=20, ncv=80).
- Итерация 2: **61/61 PASS**, префикс топ-8 при этом машинно-точен на 56/61 (диагностика).
- 10×10 спот-чеки: **4/4 PASS** (регулярный из точного кэша fig2 + 3 ревайренных с плотной
  истиной ~14 мин каждая): совпадение правой моды 1e-13–1e-14. Физпревью: уже при p=0.005
  (≈1 ребро) квадруплет подавлен, медленнейшая мода вещественная.

**Запущено:** пересчёт свипа (та же сетка p и сиды, что легаси; NUM_MODES=10; оценка ~10 ч).
Легаси-архив spectrum_sweep_10x10.npz сохранён — прямое сравнение будет в анализе.

**Дальше:** по готовым данным — редизайн Fig. 6–8 (группировка мод, осцилл./веществ. ветви),
правка текстов Sec. 7; затем блоки C–H по плану.

---

## 2026-07-20 — Блок B1 (данные + Fig. 6 v2 + Sec. 7.2): результаты пересчёта

**Свип завершён:** 1200/1200 прогонов, 0 отказов, худшая невязка мод 1.59e-12.
Архив sweep_rightmost_10x10.npz (1.76 GB, вне git; регенерация — run_sweep_rightmost.py).

**Анализ (scratchpad/analyze_sweep_b1.py, сводка в sweep_b1_summary.npz):**
- B-Q2: легаси расходится с истиной ТОЛЬКО при p<0.023 (d<Re l1> до 6.2e-4); при p>=0.023
  совпадение до 1e-16 — правая часть старых кривых валидна.
- B-Q3: кроссовер ветвей: осциллирующий лидер у 90% реализаций при p->1e-4, 50% при
  p_c~4.4e-3, 0% при p>=0.023. Заменяет фиктивный «порог p~0.005».
- B-Q4: диагональный вес осциллирующих лидеров w_diag~0.44 (рецензент прав, его 0.54 — на
  6x6); вес вещественных лидеров 0.40-0.57. IPR/overlap определены для обеих ветвей.
- B-Q5: немонотонность подтверждена: min Re l1 = -0.0845 при p~0.029, затем рост до -0.066
  при p=1 с растущим разбросом.

**Fig. 6 v2 (вариант B, одобрен):** панели (a) Re l1 исправленная; (b) доля осциллирующих
реализаций (биномиальная SE); (c) |Im l1| осциллирующей подвыборки; (d) сепарация двух
медленнейших РАЗЛИЧИМЫХ Re-групп (tol 1e-6). Новые файлы re1/oscfrac/im1/sep_p_log.pdf;
старые mode_1/2/3_p_log_ENG.pdf и gap_p_log_ENG.pdf удалены из git (содержали неверную
идентификацию мод). Скрипт fig6_lambda_visualization.py переписан (читает новый архив,
кэш fig6_rightmost.npz); определение error bars — в подписи.

**Sec. 7.2 переписана (CF-5a):** ложная скобка «purely real, Im=0» заменена честной
идентификацией max-Re; абзац наблюдений: (i) кроссовер осцилл.->веществ. с числами,
(ii) немонотонность (ускорение до p~0.03, затем замедление) + рост разброса; зелёное
предложение про «error bars растут при p~0.005» удалено (неверно — растут при p>~0.05);
«(supplemental)» -> «(not shown)». Абзац «topology-induced slow-mode localization» сохранён.

---

## 2026-07-20 — Блок B1 (Fig. 7–8 v2): групповые инвариантные локализационные метрики

**Метод:** внутри (почти) вырожденных мультиплетов индивидуальные векторы определены с
точностью до вращения базиса (та же патология, что CF-1) => IPR/overlap считаются для
РАЗЛИЧИМЫХ групп скоростей k' через инвариантный диагональный вес d_i = sum_k
|(Mat q_k)_ii|^2 (QR-ортонормировка спана группы; инвариантность под intra-group unitary
доказана; для невырожденной группы сводится ровно к легаси-определениям). Louvain СИДИРОВАН
(random_state=42, одно разбиение на граф) — закрыт и пункт блока G. Редукция:
scripts/reduce_sweep_groups.py -> precalc/fig78_groups.npz (40x30x3).

**Результаты (наука):**
- IPR лидера: 0.018 (p->0, делокализованный квадруплет ~1.8/N) -> 0.22 (p=1): рост 12x.
  История topology-induced localization сохраняется и усиливается.
- СТАРОЕ утверждение «первая мода ~в 3 раза локализованнее прочих» НЕ подтверждается на
  корректных модах: фактор <=2.1 (p=0.3), 1.5 (p=1). Текст исправлен («up to a factor of two»).
- СТАРОЕ утверждение «transient peak then decline для k=2,3 — ровно как предсказано»
  ОПРОВЕРГНУТО: ovl2 растёт монотонно 0.22->0.52; у ovl3 лишь намёк на спад при p->1 в
  пределах разброса. Текст переписан честно (без неподтверждённых предсказаний).
- Неполнота кратности квадруплета (~3.7/4 членов) безвредна для метрик: недостающий член —
  сопряжённый партнёр с тем же |diag|^2-паттерном.

**Файлы:** новые панели 10x10_IPR_group_k_1..3.pdf, 10x10_overlap_group_k_1..3.pdf
(стиль журнала: WIDTH_THIRD, Arial 8pt, компактные лог-тики); старые 6 панелей удалены из
git (неверная идентификация мод). Подписи: определение error bars (+-1 std, 30 реализаций).
Тексты Sec. 7.3–7.4: определение d_i, группы k', исправленные утверждения о контрасте.

---

## 2026-07-20 — Блок C (запуск) + D-остатки (мелкие текстовые дефекты)

**Блок C запущен:** scripts/run_advantage_control.py — dD_max для тех же 30 000 допустимых
пар (SEED=7, идентичный поток пар бенчмарку); dD_max инженерной пары = 0.000244. Числа CF-3
пойдут в текст Sec. 8.4 после завершения счёта.

---

## 2026-07-20 — Блок C завершён (CF-3: честная сила эффекта + контроль преимущества)

**Расчёт (30 000 пар, SEED=7, тот же поток, что бенчмарк):** dD_max инженерной пары =
2.44e-4; больше — у 41/30000 (0.137%) => 99.86-й перцентиль. Медиана ансамбля 2.2e-15
(машинный ноль — типичная пара не обгоняет вовсе), p90=1.2e-8, p99=6.0e-6, max=8.3e-3.
Данные: precalc/dirichlet_advantage_30k.npz.

**Правки текста:**
1. Sec. 8 (строка 557): «engineer a macroscopic effect» -> «robust, physically observable
   and statistically exceptional» — снято завышение, отмеченное рецензентом (CF-3).
2. Sec. 8.4 (после предложения про 1.2%): добавлен честный количественный пассаж —
   D(t*) ~ 3.6e-3 (пересечение близко к равновесию, число проверено аудитом ранее),
   dD_max ~ 2.4e-4, «absolute values are necessarily small», НО 99.86-й перцентиль
   ансамбля и медиана на уровне шума. Никаких новых недоказанных утверждений — все
   числа из расчёта.
Компиляция: 31 стр., 0 ошибок/undefined.

---

## 2026-07-20 — Блок G: тестовый контур (заявленное сделано реальным)

**Проблема (рецензент):** README командовал `pytest`, equivalence_notes перечислял
«guardrails» — тестов не существовало; плюс в README жил протухший macOS-путь
(/Users/alpha7en/...).

**Сделано:**
- `tests/test_kernels.py` (9 быстрых): топология (сид, связность, число рёбер),
  размерность лиувиллиана, **точное** совпадение генератора с линдбладианом H=−(1/2)τ
  (J-конвенция), дефазинг действует только на когерентности, пределы entropic_distance
  и IPR, валидность Дирихле-состояния, невязка точной проекции на вырожденной решётке
  (контроль CF-1), базис-инвариантность группового веса (метрика Fig. 7–8).
- `tests/test_artifacts.py` (4): отпечатки канонического графа; согласованность pkl↔npz
  (Хаусдорф спектров); бит-повтор первых разрешённых пересечений бенчмарка (SEED=7);
  skipif на свежем клоне без pkl.
- `tests/test_slow.py` (2, маркер slow): полная 5-проверочная пересборка валидатора;
  rightmost-решатель против плотной истины на регулярной 6×6 (режим отказа CF-2).
- `conftest.py` (пути), `pytest.ini` (slow deselected по умолчанию).
- README: команды тестов исправлены, macOS-путь удалён; equivalence_notes: список
  guardrails переписан под реально существующие тесты.

**Прогон:** fast 13/13 passed (11 c); slow 2/2 passed (38 c).

**D-остатки (6 правок main.tex):**
1. Строка 186: смешанная запись d rho/dt = L(rho) = L·|rho>> исправлена на d|rho>>/dt =
   L|rho>> — это место, которое имел в виду ещё ПЕРВЫЙ рецензент (#6); ранее была найдена
   только корректная форма в Sec. 3.1.
2. Пустой \textbf{} (строка 184) удалён.
3. «Mixed (Top Edges)... along the top boundary edges» -> «Top/Bottom Edges... top and
   bottom boundary rows» — сверено с генератором create_top_bottom_edges_state (код
   заполняет обе граничные строки); согласовано с легендой Fig. 3 «Edges (t/b)».
4. c_0=1: добавлено однострочное обоснование (<<sigma_0|rho>> = Tr rho = 1).
5. \label{fig:placeholder} (нигде не цитировался) переименован в fig:lavg_selection.
6. Удалены 4 строки-заглушки закомментированных russian-блоков.
Компиляция: 30 стр., 0 ошибок/undefined.

---

## 2026-07-20 — Блок E: литература и атрибуция

**Добавлено в refs.bib (8 записей, метаданные верифицированы веб-поиском):**
Lu2017 (PNAS 114, 5083 — механизм марковского Мпембы), Klich2019 (PRX 9, 021060 — strong
Mpemba), Ares2023 (Nat Commun 14, 2036 — entanglement asymmetry, теория к эксперименту
Joshi2024), Chatterjee2023 (PRL 131, 080402 — квантовая точка), Moroder2024 (PRL 133,
140404 — термодинамика QME), AresReview2025 (Nat Rev Phys 7, 451 — обзор), Whitfield2010
(PRA 81, 022323 — quantum stochastic walks), fiedler1973 (алгебраическая связность).

**Правки текста (6 мест):** механизм атрибутирован Lu&Raz + Klich (классика) до Carollo
(квантовый перенос); обзор процитирован во введении; Ares2023 добавлена к абзацу про
entanglement asymmetry (раньше цитировался только эксперимент); предложение про
термодинамику (Chatterjee2023, Moroder2024) после Zhang2025; QSW-классификация модели
(Whitfield2010) в Sec. 2; связность↔нуль-моды (fiedler1973) в Sec. 4; breuer2002 + address.

**Проверено:** существующая Zhang2025 корректна (Nat Commun 16, 301 (2025) — DOI c «2024»
это норма нумерации). Компиляция: 30 стр., 0 undefined citations.

---

## 2026-07-20 — Блок F: физическая адекватность модели

**Верифицировано перед правкой:** генератор кода ≡ линдбладиан с H = −(1/2)τ (J_phys = 1/2,
vec-столбцовая свёртка), совпадение матриц 0.0 на тестовом графе — заявление статьи «J=1/2»
внутренне консистентно; квирк «код j=1.0 ↔ физический J=1/2» задокументирован в REPRODUCING.md.
Утверждение о равномерных потерях проверено аналитически: в однофотонном секторе dρ/dt =
L₀ρ − κρ ⇒ ρ(t)=e^{−κt}e^{L₀t}ρ(0) — нормированная динамика и все пересечения не меняются.

**Правки текста (5 мест, все в main.tex):**
1. Sec. 2: время ↔ длина распространения в волноводных решётках (одно предложение).
2. Sec. 2: физическое обоснование дефазинга (флуктуации константы распространения;
   доминирующий канал декогеренции в фотонных реализациях, cit. Longhi2024) + честное
   утверждение про равномерные потери (факторизуются, нормированная динамика неизменна)
   и границу применимости (site-resolved losses — вне рамок).
3. Sec. 3: скобка про единицы времени (обезразмеривание, связь с обратной константой связи).
4. Sec. 4: раскрытие кондиционирования ансамбля связностью с числами из
   check_connectivity_stats.py (acceptance ≈1 при p≲0.05, ≈86% при p=0.3, ≈20% при p=1).
5. Заключение: потери с разрешением по узлам добавлены в future work.

Компиляция: 30 стр., 0 undefined.
